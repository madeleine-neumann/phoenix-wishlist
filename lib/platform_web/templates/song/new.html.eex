<h1 class="headline__big">Add new Song</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.song_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.song_path(@conn, :index), class: "button button__back" %></span>
